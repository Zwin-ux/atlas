---
name: atlas-voxel-map
description: Use when building or reviewing the Atlas voxel county map widget, isometric map state, nodes, layers, Clawd route, and visual grammar.
---

Build a fake-but-beautiful Alpha voxel map using React + SVG/Canvas. Do not use Three.js unless explicitly requested later. Use county pack data. The widget renders state; it does not invent source truth.

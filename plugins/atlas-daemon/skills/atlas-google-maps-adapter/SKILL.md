---
name: atlas-google-maps-adapter
description: Use for Google Maps Platform integration behind GeoDataAdapter: geocoding, nearby search, aggregate counts, routes, and data safety.
---

Build mock-first. Real Google adapter must compile but not be required for Alpha. Use minimal field masks. Do not store full Google payloads by default. Convert signals into Atlas data structures.

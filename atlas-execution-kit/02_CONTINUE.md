# Continue Atlas from the last verified slice

Resume the current `Zwin-ux/atlas` effort using `atlas-execution-kit/01_PASTE_IN_GROK.md` as the assignment. Do not re-plan from scratch or reopen retired scope.

First inspect repo identity, actual Git state, active worktrees/sessions, and changed instructions. Read the checkpoint in `docs/STATUS.md` and the task linked from the current project plan. Revalidate existing receipts against the actual checkout, dependency evidence, and any changed acceptance definitions. Never trust old “green” prose over missing artifacts.

Do not launch duplicate builders. Reconcile interrupted work and in-flight ownership before assigning a new task. Preserve useful uncommitted changes and inspect before any recovery action. A crashed workflow must not silently release ownership while its child processes still run.

Use the graph planner's ready queue with the real receipt directory, repo root, active task IDs, declared blockers and required invalidations from the checkpoint. Continue the smallest ready, high-priority task. Bypass a blocked human lane only by doing independent work—not by skipping its gate or inventing proof.

If a node is already implemented, prove its acceptance instead of rewriting it. If a receipt is stale, rerun only the affected checks and required downstream gates. If no work is ready, report the exact blocking dependency, missing permission, or missing capability and the smallest next action.

Do not end after a status recap when safe implementation remains. Complete one verified slice, integrate it, record evidence, update the existing checkpoint, and select the next ready node within the authorized session.

# Task packet / fill from the graph, keep it small

Task ID and definition hash:
User-visible outcome:
Owner role and actual session:
Independent reviewer session:
Repo root / worktree / branch:
Base revision and current dirty-state inspection:
Accepted prerequisite receipt hashes:

## Scope
Exact allowed write paths (not just broad module names):
Forbidden paths/actions:
Shared contract snapshot and source:
Exclusive resource locks:
Owned test server target/port/process:
Child budget / stop condition:

## Focused context
Current authority references:
Relevant source paths and excerpts:
Related requirement and case IDs:
Known failure reproduction:
Failed hypotheses not to repeat:

## Acceptance
Copy/link the node's specific criterion IDs and assertions.
For each, identify a proposed test/procedure and expected evidence.
Do not run unsupported commands just because a previous agent suggested them.

## Deliverable
Diff/commit and exact changed-file list; commands with real exit codes; inspected visual/host artifacts if relevant; unmet acceptance; risk to downstream contracts; no blanket release claim.

## Integration ownership
Builder does not merge or write shared status. Lead reviews, integrates only intended changes, reruns affected checks, and accepts a receipt on the integrated revision.

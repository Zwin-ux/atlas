## Execution checkpoint

<!-- Embed a compact version in existing docs/STATUS.md. Do not create another status file. -->
Current scope/decision reference:
Graph file and current node:
Accepted receipt directory/reference (no secrets):
Active task → owner/session → worktree/base → held files/locks:
Declared blocked nodes and exact reasons:
Invalidated evidence nodes and reason:
Last verified slice and actual evidence:
Uncommitted work to preserve:
Failed hypotheses / checks still unrun:
Next task and exact command:
External owner gate and minimal action:

Keep changing SHAs/URLs/counts in linked machine-readable artifacts rather than copying them into multiple prose locations. Follow the existing status length cap and registry policy.

### Final freeze rule

Before final exact-revision tests, commit intended code/config/docs and prepare status links to the evidence location. During the final write-free verification window, store run evidence outside tracked product content and report outcomes through the linked receipts/terminal handoff. Do not make a harmless status edit and then claim the old exact-HEAD host proof still applies. If tracked content changes, re-freeze and refresh required final proof.

# 0.75R Handoff Notes (live — updated as work proceeds)

**Purpose:** if the current agent session dies, a successor (codex or Claude)
continues the 0.75R release from here. Read this whole file before touching
the repo.

## The mission (0.75R plan summary)

Ship line: merge `fable/0.58e-prop-cleanup` (0.73F+0.74F engine superpass)
into canonical `codex/integrate-hosted-clawd-fable-058e`, claymation identity
pass, submission hardening, deploy to existing Railway
(`atlas-backend-production-e6fc.up.railway.app`), produce submission packet.
Full plan text: see the Plan-agent result inside transcript
`~/.claude/projects/C--Users-mzwin/740b05a5-7dfa-4978-81ff-4cbe7a702164.jsonl`
line 2410 (or ask the user). Stages:

- **Stage 0** — safety snapshot + canonical reconciliation. ✅ **DONE**
  (commit `c27be35`, tags `pre-0.75r-fable` + `pre-0.75r-canonical`, both
  branches + tags pushed; frozen-lockfile install, typecheck, tests passed).
- **Stage 1** — merge fable → canonical. 🔶 **IN PROGRESS — see below.**
- **Stage 2** — claymation reference board (research → docs/design/CLAY_TOKENS.md).
- **Stage 3** — claymation implementation (3a chrome styles.css, 3b scene
  CityWorldRenderer only; no core shading changes).
- **Stage 4** — submission hardening (real 512px icon, /support route,
  screenshots, extend verify-submission.mjs, CSP frame-ancestors on /preview,
  manifest copy pass).
- **Stage 5** — deploy (HUMAN gate) + prod verification + submission packet
  doc (HUMAN gate).

## Where Stage 1 stands right now

Worktree `C:\Users\mzwin\Documents\Atlas`, branch
`codex/integrate-hosted-clawd-fable-058e`, **merge in progress**
(MERGE_HEAD exists, all conflicts resolved & staged, NOT yet committed).

Done and verified in this session:
- All 9 conflicted files resolved. Renderer = fable base + canonical deltas
  re-ported (rAF camera coalescing, pointer-capture try/catch ×3,
  rebuildCountRef, 0.67H product-feel calm, 0.68H buildingFaceColor).
- `pnpm install --frozen-lockfile` ✅, `pnpm typecheck` (all workspaces) ✅
- `pnpm test:core` **107/107** ✅ (after expectation rebases below)
- test:hosted-clawd 15/15, billing, protected-tool-gate, saved-read ✅
- build:web + build:geo ✅; server runs on :8793
- verify-generated-district-parity ✅, verify:widget:perf ✅
- verify-generated-district-widget ✅ — **rewritten** to the 0.75R host-driven
  contract (calls MCP render_voxel_county includeGeneratedDraft:true, injects
  result via `atlas:test-tool-result`; banner-copy predicate updated to
  "generated / not real coverage / nothing is saved").
- 21-script renderer-coupled sweep: all ✅ except alpha-rc-split (expected —
  it audits the uncommitted tree; re-run AFTER the merge commit) — and
  graphics-cleanup now ✅ after token rebase (0.67H literals collapsed by the
  fable focus-overlay architecture; tokens updated in the verifier).
- Deliberate expectation rebases (all commented in-code with 0.75R markers):
  - `packages/core/test/city-world-generated-district.test.ts` — lots>15,
    buildings>15, places>=6 (fable generator makes fewer/larger lots).
  - `scripts/verify-deterministic-generated-district-specs.mjs` — same floors
    + token `maxPropCommands: 20`.
  - `packages/core/src/voxel/cityWorldSceneWindow.ts` —
    generated_draft_window maxPropCommands 12→20 (0.74F prop kit reaches
    drafts; observed 13-16).
  - `scripts/verify-city-world-graphics-cleanup.mjs` — 4 tokens respelled.
  - `scripts/verify-generated-district-widget.mjs` — new contract (above).

**OPEN ISSUE (currently being investigated):** two canonical browser gates
fail: `verify:hosted-clawd-save-ux:browser` (save-strip needs tray expanded;
tray now opens as collapsed peek) and `verify:hosted-clawd-browser-proof`
(expects hosted state "active" from injected fixture but widget shows
default "confirm_save"; expects saved-read HTTP 401 + WWW-Authenticate
challenge, server returns 200; expects 0.65H-era copy "no auth or database
adapter is active in this scaffold"). Hypothesis: **pre-existing drift inside
canonical's own 0.62H–0.68H dirty train**, NOT merge damage (all involved
files are canonical-only). A scratch worktree
`C:\Users\mzwin\Documents\atlas-premerge-check` at tag `pre-0.75r-canonical`
was just created to prove it: install, build:web, run server on :8794 with
`ATLAS_HOSTED_CLAWD_PERSISTENCE_ENABLED=1`, run both gates with
`--url http://127.0.0.1:8794/preview`. If they fail identically → record as
known pre-existing in BUILD_LOG, do NOT block the merge commit; fix in a
follow-up slice. If they PASS on pre-merge → the merge broke them, bisect
the difference (likely web bundle / scene id) before committing.
**Remove the scratch worktree afterward:**
`git -C C:\Users\mzwin\Documents\Atlas worktree remove ..\atlas-premerge-check --force`.

## Remaining Stage 1 checklist

1. Resolve the two hosted-clawd gates (per above).
2. `git add -A` (incl. verifier/test/docs edits + artifacts/0.75r-merge/*)
   and `git commit` the merge. Suggested message:
   `merge(0.75r): fable 0.73F+0.74F engine superpass into hosted-clawd canonical`
   with body listing the expectation rebases above. (Merge commits: plain
   `git commit` completes the in-progress merge.)
3. Re-run `node scripts/verify-alpha-rc-split.mjs` on the clean tree.
4. Push, tag `0.75r-merged`, push tag.
5. Update docs: STATE.md is already rewritten to post-merge truth (staged);
   append BUILD_LOG entry for 0.75R Stage 1 listing verification + rebases.

## Environment gotchas (Windows box)

- **pnpm not on PATH.** Shim exists:
  `$env:PATH = "C:\Users\mzwin\AppData\Local\Temp\claude\C--Users-mzwin\58274080-ecd4-49b8-a983-8cf7c8e77a4a\scratchpad\bin;$env:PATH"`
  (pnpm.cmd = `corepack pnpm %*`). Must be set in the SAME PowerShell call.
- Server: `$env:PORT=8793; pnpm exec tsx server/src/index.ts` (background).
  It CACHES the web bundle in memory — after `pnpm build:web` you MUST
  restart it. A server may currently be running on :8793 with
  `ATLAS_HOSTED_CLAWD_PERSISTENCE_ENABLED=1`.
- Verifier scripts default to :8787 — set
  `$env:ATLAS_PREVIEW_URL = "http://127.0.0.1:8793/preview"` (most honor it).
- gstack browse binary: `~\.claude\skills\browse\dist\browse.exe`.
- The 0.75R plan's authoring session (740b05a5…) and its scratchpad
  (`…\Temp\claude\C--Users-mzwin\740b05a5-…\scratchpad`) hold the canonical
  renderer delta diff + baseline screenshots.

## Hard rules from the plan

- Never touch core shading in Stage 3 (keeps face-contrast gate green).
- Renderer recolors: tables/shader only, don't rename string-coupled symbols
  (verify-alpha-rc-split, civic-venue, commerce-strip grep renderer source).
- If a verifier expectation must change, change it in the same commit and
  record it in BUILD_LOG.
- Hosted Clawd flags stay OFF for the production deploy.
- HUMAN gates: Railway deploy trigger, OpenAI submission click.

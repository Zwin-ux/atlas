# Plan: WIRE REAL GEOGRAPHY LIVE (0.78 focus) + deploy backlog + 0.80 parallel

## Context

Owner picked (2026-07-12): make real geography the next focus, and deploy the
certified backlog now. The county geo compiler (`compileCountyGeoScene`,
committed `c116c1b`, tested) is ORPHANED — nothing in server/web consumes it,
so real county shapes produce zero user-visible output. Wiring it is the
direct answer to the standing critique "the county isn't accurate." Caveat
that shapes the wire: the compiler currently emits ONLY flat grass tiles — so
connecting it as-is renders a featureless green blob. The wire MUST add real
water from the pack (Miami-Dade's bay, Kalawao's sea cliffs) or the first
result is worse than today's generated preview. 0.80 graphics (grade+lighting)
runs in parallel.

## Phase 0 — Deploy the certified backlog (NOW; human deploy)
`scripts/release-deploy.ps1` ships everything committed since the P1 release:
P1.3 map-first questions, the G8-7 texture-CSP fix (unblocks curated textures
in real ChatGPT), the 0.79 worst-tail visual lift + landmark-in-frame, and the
dormant geo compiler. Real-host users get the texture fix immediately. All
release-gated; low risk.

## Phase 1 — Real county boards, rendered WELL (0.78-1)
1. **Bake water** — extend `scripts/build-county-geo-packs.mjs` to include a
   WATER layer per county (TIGER AREAWATER + coastline within the county
   envelope) beside the boundary ring; simplify to a vertex budget; keep LOD0
   packs small (<12KB). Then bake ALL 3,222 (network/build-time, my machine,
   politeness-throttled ~350ms/county).
2. **Compiler water** — extend `packages/core/src/voxel/cityWorldCountyGeoScene.ts`
   to classify each land tile as grass vs WATER by testing the tile against
   the pack's water polygons; add a shore band at the land/water boundary and
   a board-edge bevel so the silhouette reads land-vs-sea. Deterministic +
   new tests (Miami-Dade has substantial water; Loving ~none).
3. **Wire** — server attaches the county geo pack to the shell-county tool
   result `_meta` (small, same spec-not-scene pattern as 0.76-T); widget
   `web/src/App.tsx` compiles + renders the county board as the PRIMARY county
   view when a pack is present; the generated district demotes to the
   town-detail drill-down (Phase 3).
4. **Gates** — determinism; existing silhouette-fidelity; NEW water-from-pack
   presence check; emulator audit/perf at county scale. The board is
   terrain-heavy (banded, cheap) so likely UNDER the 1600 ceiling — measure;
   only if it exceeds does 0.78-R renderer LOD become a prerequisite.

## Phase 2 — Real towns as anchors (0.78-2)
1. Extend packs with TIGER PLACE (real incorporated towns) + the county seat,
   top-N by population, within the county.
2. Compiler + widget place towns as tappable `CityWorldPlace` anchors on the
   board; county seat marked distinctly; labels use the tiered styling
   (Program B) with REAL Census names — the county board finally TELLS you
   what you're looking at.
3. `ask_county_question` routes through real towns ("where is Homestead?").

## Phase 3 — Town detail (0.78-3)
Tap a town → the existing generated-district engine, re-anchored + titled with
the real town name; camera transitions county↔town via the preset machinery;
widget state per scene id (P1.1 sceneSessions).

## Parallel — 0.80 graphics + Program B labels (running/queued)
0.80-1 grade+lighting is in flight; 0.80-2..6 (water material, road recede,
palette vibrancy, composition, aesthetic score signals) + Program B tiered
labels continue as fenced packets — they improve BOTH the generated engine and
the new geo board.

## Deferred (unchanged, in docs/PRODUCTION_ROADMAP.md)
Personal Atlas / persistence (C1-C4; storage built, needs owner Auth0 setup);
0.78-R renderer LOD (reviewer hands-on; becomes required only if the all-roads
max-detail phase or big county boards blow the ceiling); worker healthcheck
(P0.4, Railway dashboard, human); directory submission (P4, held for the bar).

## Critical files
- `scripts/build-county-geo-packs.mjs` — add water + places layers, bake 3,222.
- `packages/core/src/voxel/cityWorldCountyGeoScene.ts` — water tiles, shore,
  board edge, town anchors (reuse the existing point-in-polygon + iso-lattice
  fill already there).
- `server/src/index.ts` — attach geo pack to shell-county `_meta` (reuse the
  `generatedDraftSpec` attach pattern).
- `web/src/App.tsx` — compile + render county board; town drill-down (reuse the
  client-compile + `sceneSessions` patterns).
- `data/geo-packs/*` — baked packs (16 exist; expand to 3,222).

## Verification
1. `node scripts/build-county-geo-packs.mjs --challenge` — 16 packs with water;
   spot-check coastal has water, Loving has ~none; run twice → identical.
2. `pnpm test:core` — geo compiler determinism + water classification tests;
   curated Riverside byte-identity untouched.
3. `scripts/certify.ps1` — full ladder incl. emulator audit/perf at county
   scale (Graphics ≤1600; LOD only if exceeded).
4. Crop test (my eyes → owner screenshots): Miami-Dade reads as Miami-Dade WITH
   its bay/coast; Loving = dry rectangle; Kalawao = sea-cliff peninsula.
5. G8 real-host after deploy: "show me Miami-Dade" → recognizable real county
   board; tap a town → detail. Owner screenshot approval (human gate).

# Keep the Railway upload to build/runtime inputs only. Everything below is
# docs, screenshots, prompts, or reference art — not needed to build or run the
# server, and it was bloating the upload enough to time out.
artifacts/
docs/
prompt-pack/
engineering-prompt-pack/
prompts/
experiments/
assets/reference/
.reference/
.codex/
.gstack/
.github/
.claude/

# Baked TIGER road-chunk payloads (163 MB). The 2D atlas reads only
# _coverage.json from this directory, to state honestly which counties have
# real street geometry. The chunk bytes themselves serve the retired
# /road-chunks routes and were pushing the upload past what Railway accepts.
data/road-chunks/*
!data/road-chunks/_coverage.json

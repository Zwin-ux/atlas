---
name: atlas-voxel-renderer
description: Use for VoxelScene, PixiJS renderer, map node rendering, Clawd route, and light voxel board UI.
---

Renderer consumes VoxelScene only. It must not calculate business logic. Use PixiJS if clean, fallback to Canvas/SVG if needed. Keep the map lighter and readable.
